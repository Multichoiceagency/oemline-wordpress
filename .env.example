WORDPRESS_DB_HOST=db
WORDPRESS_DB_USER=wordpress
WORDPRESS_DB_PASSWORD=change_me
WORDPRESS_DB_NAME=wordpress
WORDPRESS_TABLE_PREFIX=wp_
WORDPRESS_DEBUG=false
MYSQL_ROOT_PASSWORD=change_me_root

# WordPress site URL (must match Coolify domain)
WORDPRESS_URL=https://wp.oemline.eu

# WordPress config extras (JWT, SSL, etc.)
WORDPRESS_CONFIG_EXTRA=define("WP_HOME","https://wp.oemline.eu");define("WP_SITEURL","https://wp.oemline.eu");define("JWT_AUTH_SECRET_KEY","change_me_jwt_secret");define("JWT_AUTH_CORS_ENABLE",true);define("FORCE_SSL_ADMIN",true);define("FS_METHOD","direct");define("WP_MEMORY_LIMIT","256M");if(isset($_SERVER["HTTP_X_FORWARDED_PROTO"])&&$_SERVER["HTTP_X_FORWARDED_PROTO"]=="https"){$_SERVER["HTTPS"]="on";}

# OEMline dashboard integration
DASHBOARD_API_URL=https://api-bsg4wgow80c8k4sc404ko00k.oemline.eu
DASHBOARD_API_KEY=change_me
OEMLINE_WP_API_KEY=change_me
REVALIDATION_SECRET=change_me

# MinIO / S3 media storage
MINIO_ENDPOINT=https://s3.oemline.eu
MINIO_ACCESS_KEY=change_me
MINIO_SECRET_KEY=change_me
MINIO_BUCKET=wordpress-media
MINIO_PUBLIC_URL=https://s3.oemline.eu/wordpress-media

# Next.js storefront
NEXTJS_BASE_URL=https://oemline.eu
